/// The three source types for wool collection.
enum SourceType {
  /// C1 — direct shepherd shearing
  c1,

  /// C2 — slaughterhouse wool-on-skins (auto-urgent)
  c2,

  /// C3 — third-party aggregator
  c3,
}
